<h1>Détails du logement</h1>

<p>Prix : <?php echo $accommodation['price']; ?> EUR</p>
<p>Description : <?php echo $accommodation['description']; ?></p>

<h2>Réserver ce logement</h2>
<form method="POST" action="/rental/create">
    <input type="hidden" name="id_accommodation" value="<?php echo $accommodation['id']; ?>">
    <label>Date de début : <input type="datetime-local" name="date_from" required></label>
    <label>Date de fin : <input type="datetime-local" name="date_to" required></label>
    <button type="submit">Réserver</button>
</form>
<h1>Réservations pour ce logement</h1>
<ul>
    <?php foreach ($reservations as $reservation): ?>
        <li>
            <p>Réservé par : <?php echo $reservation['customer_name']; ?></p>
            <p>Du : <?php echo $reservation['date_from']; ?> au : <?php echo $reservation['date_to']; ?></p>
        </li>
    <?php endforeach; ?>
</ul>